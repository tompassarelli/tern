{
  "name": "north-sdk",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dispatch": "bun run src/dispatch.ts",
    "check": "bun x tsc --noEmit && bun build src/dispatch.ts --no-bundle > /dev/null && bun build src/spawn.ts --no-bundle > /dev/null && bun build src/integrations/linear/cli.ts --no-bundle > /dev/null"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.195"
  }
}
